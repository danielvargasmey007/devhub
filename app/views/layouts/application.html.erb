<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Devhub" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Devhub">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  </head>

  <body>
    <header style="background: #333; color: white; padding: 10px 20px; margin-bottom: 20px;">
      <div style="display: flex; justify-content: space-between; align-items: center;">
        <div>
          <strong><%= link_to "DevHub", root_path, style: "color: white; text-decoration: none;" %></strong>
        </div>
        <nav>
          <% if logged_in? %>
            <span style="margin-right: 15px;">Logged in as <strong><%= current_user.name %></strong></span>
            <% if current_user.admin? %>
              <%= link_to "Admin", admin_path, style: "color: #ffd700; text-decoration: none; margin-right: 15px;" %>
            <% end %>
            <%= button_to "Logout", logout_path, method: :delete, style: "display: inline; background: #dc3545; color: white; border: none; padding: 5px 10px; border-radius: 3px; cursor: pointer;" %>
          <% else %>
            <%= link_to "Login", login_path, style: "color: white; text-decoration: none; margin-right: 10px;" %>
            <%= link_to "Sign Up", signup_path, style: "color: white; text-decoration: none;" %>
          <% end %>
        </nav>
      </div>
    </header>

    <% if flash[:notice] %>
      <div style="background: #d4edda; color: #155724; padding: 10px 20px; margin: 0 20px 20px; border: 1px solid #c3e6cb; border-radius: 4px;">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <% if flash[:alert] %>
      <div style="background: #f8d7da; color: #721c24; padding: 10px 20px; margin: 0 20px 20px; border: 1px solid #f5c6cb; border-radius: 4px;">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <%= yield %>
  </body>
</html>
